package uk.ac.belfastmet.examples;

import java.io.*;

public class balancedBrackets {
// local variable
/**.
 * Input exp = "[()]"
 * Output Balanced
 * 
 * Input: exp "{[]}" 
 * Output Balanced
 * 
 *  Input exp = "[] {} ()"
 *  Output Balanced
 *  
 *  Input: exp "[)[)""
 *  Output Not Balanced
 *  
 *   Input: exp = "()]()"
 *   Output Not balanced
 * 
 * 
 * BalancedBrackets.**/
	
/**.
 * Ok so what's the logic? How to measure if they are balanced? try an if statement? I need the string length, i need to see if the string 
 * is odd if so it is not even. If even I need to split the string - where? I need to compare these strings if so do they balance?
 * 
 * 
 * 
 * ..**/
	public String strLength() {
		String strBrac1 = "[()]";//Balanced
		String strBrac2 = "{[]}";//Balanced
		String strBrac3 = "[] {} ()";//Balanced
		String strBrac4 = "[)[)";//Not Balanced
		String strBrac5 = "()]()";//Not Balanced
		
		
		public int length() {
		int length1 = strBrac1.length();
		int length2 = strBrac2.length();
		int length3 = strBrac3.length();
		int length4 = strBrac4.length();
		int length5 = strBrac5.length();
		}
		
		return StrResult = "";
	}
	
	}
	